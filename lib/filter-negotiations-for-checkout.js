import _ from 'underscore';

const filterNegotiationsForCheckout = (negotiations) => {
    // Group all offers by groupId and save them in a variable
      return negotiations;
    
  
    return groupedNegotiations;
};

export default filterNegotiationsForCheckout;
